name: Deploy POC a GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm, cache-dependency-path: poc/package-lock.json }
      - run: npm ci
        working-directory: poc
      - run: npm run build
        working-directory: poc
      # el sitio publicado lleva el POC en la raíz y el export de flujos en /flujos
      - run: |
          mkdir -p _site
          cp -r poc/dist/* _site/
          cp -r mobbin-export _site/flujos
      - uses: actions/upload-pages-artifact@v3
        with: { path: _site }

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
